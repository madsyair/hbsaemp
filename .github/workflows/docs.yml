name: docs

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    # Mondays 03:00 UTC — only the linkcheck job runs on this trigger.
    - cron: "0 3 * * 1"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Never let two Pages deployments run at once, and never cancel one midway:
# a cancelled deploy can leave the live site in a half-updated state.
concurrency:
  group: pages
  cancel-in-progress: false

env:
  # The tutorials call the diagnostics helpers, which plot. Without a headless
  # backend a CI runner has no display and the notebook execution dies.
  MPLBACKEND: Agg

jobs:
  build:
    if: github.event_name != 'schedule'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      # 3.12 is the package's declared floor (`requires-python = ">=3.12"`).
      # Building on the floor proves the floor is honest; newer versions then
      # follow for free.
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip

      # PyTensor compiles C extensions on first use and caches them here.
      # Without this the cost is paid again on every single run.
      - name: Cache PyTensor compiledir
        uses: actions/cache@v5
        with:
          path: ~/.pytensor
          key: pytensor-${{ runner.os }}-py3.12-${{ hashFiles('pyproject.toml') }}
          restore-keys: pytensor-${{ runner.os }}-py3.12-

      # The [bambi] extra is required from v0.2 on: `nb_execution_mode` is
      # "force" when CI is set, so the tutorials genuinely sample rather than
      # being rendered as dead text.
      - name: Install package and documentation toolchain
        run: pip install -e ".[bambi]" -r docs/requirements-docs.txt

      # -W turns every warning into an error. --keep-going is deliberately
      # absent: it has been the default since Sphinx 8.1.
      - name: Build documentation
        run: sphinx-build -b html -W docs/source docs/_build/html

      # `actions/configure-pages` is deliberately not used. It only supplies a
      # base path for generators that need one, which Sphinx does not, and it
      # fails until Pages has been switched on in Settings. Leaving it out
      # keeps this job green on pull requests from the very first run.
      - uses: actions/upload-pages-artifact@v5
        with:
          path: docs/_build/html

  # Runs the code examples that are cheap and deterministic. Anything that
  # samples is marked +SKIP in the pages themselves; proof that MCMC still
  # works comes from the executed tutorials in `build`, not from here.
  doctest:
    if: github.event_name != 'schedule'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
      - name: Install package and documentation toolchain
        run: pip install -e ".[bambi]" -r docs/requirements-docs.txt
      - name: Run documentation doctests
        run: sphinx-build -b doctest docs/source docs/_build/doctest

  # Deliberately not a pull-request gate: a third-party site being down is not
  # a reason to block a merge. Weekly, plus on demand.
  linkcheck:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
      - name: Install package and documentation toolchain
        run: pip install -e ".[bambi]" -r docs/requirements-docs.txt
      - name: Check external links
        run: sphinx-build -b linkcheck docs/source docs/_build/linkcheck

  deploy:
    needs: build
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
